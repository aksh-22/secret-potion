import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const CommentChangeModal = (props: Props) => {
    return (
        <View>
            <Text>CommentChangeModal</Text>
        </View>
    );
};

export default CommentChangeModal;

const styles = StyleSheet.create({});

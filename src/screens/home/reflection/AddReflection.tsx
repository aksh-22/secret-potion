import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const AddReflection = (props: Props) => {
  return (
    <View>
      <Text>AddReflection</Text>
    </View>
  );
};

export default AddReflection;
